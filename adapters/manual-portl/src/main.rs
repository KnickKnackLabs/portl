//! portl-manual-adapter.
//!
//! Reference "adapter" that doesn't orchestrate anything — it
//! prints step-by-step instructions for a human to provision a
//! target and enroll it with portl. Real content is filled in as
//! the protocol lands; M0 ships a stub.

fn main() {
    println!("portl-manual-adapter: stub; instructions filled in during M2+");
}
